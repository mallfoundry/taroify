export enum FieldType {
  Number = "number",
  Text = "text",
  Idcard = "idcard",
  Digit = "digit",
  Textarea = "textarea",
  Password = "password",
}

export type FieldTypeString = "number" | "text" | "idcard" | "digit" | "textarea" | "password"
