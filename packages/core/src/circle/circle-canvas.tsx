import { View } from "@tarojs/components"
import * as React from "react"
import { CircleProps } from "./circle.shared"

function CircleCanvas(props: CircleProps) {
  return <View>weapp</View>
}

export default CircleCanvas
